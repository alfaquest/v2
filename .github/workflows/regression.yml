name: Regression tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Private repos on GitHub Free allow only one concurrent job. A per-branch
# concurrency group lets multiple pushes queue separate runs that all wait for
# a runner and then fail after ~15 minutes. One repo-wide group cancels stale
# runs immediately when a newer commit arrives.
concurrency:
  group: regression-tests-${{ github.repository }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chrome
        run: npx playwright install chrome --with-deps

      - name: Run regression tests
        run: npm test

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  deploy:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Required repo secrets (Settings → Secrets and variables → Actions):
      #   CLOUDFLARE_API_TOKEN — Cloudflare API token with "Cloudflare Pages: Edit"
      #   CLOUDFLARE_ACCOUNT_ID — e.g. 06d216065309310baafe29018273f4b2
      # If deploy is not configured, use manual deploy: npm run deploy
      - name: Deploy to Cloudflare Pages (production)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4'
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          command: pages deploy public --project-name=alfaquest-pages --branch=main --commit-hash=${{ github.sha }}
